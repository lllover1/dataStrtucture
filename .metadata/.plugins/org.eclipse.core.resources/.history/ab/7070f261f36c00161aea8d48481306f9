package parttern;

public class Utils {
	public  int BruteForce(String target,String parttern){
		int partternLength = parttern.length();
		int targetLength = target.length();
		if(partternLength==0||targetLength<)
		for(int i =0;i<partternLength;i++){
			for()
		}
		return 0;
	}

}
