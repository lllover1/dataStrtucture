package parttern;

public class Utils {
	public  int BruteForce(String target,String parttern){
		int pLength = parttern.length();
		int tLength = target.length();
		if(pLength==0||tLength<partternLength)
		for(int i =0;i<partternLength;i++){
			for()
		}
		return 0;
	}

}
